# Personal data files
*.xlsx
*.pdf
# Contents only, not the directory: git cannot re-include a file whose parent
# directory is excluded, and the generated builtin label pack has to be
# committed (the 21MB dump it comes from must not be).
backend/data/*
!backend/data/builtin-address-labels.json
# The bridge pack is the research record, not a cache of one: the source URLs
# and explorer name tags behind each address live nowhere else.
!backend/data/builtin-bridge-labels.json

# OS junk
.DS_Store

# Google Sheets export (contains API keys)
google-sheet-export/

# Claude Code
.claude/
.playwright-mcp/
node_modules/
docs/quickstart/

.claude/settings.local.json

todo.md